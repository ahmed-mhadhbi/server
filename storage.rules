rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Menu images - readable by all, writable by authenticated staff
    match /menu/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

